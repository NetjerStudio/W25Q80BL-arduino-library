# W25Q80BL-arduino-library
A library for a flash memory W25Q80BL for Arduino.

To install : Download all the repository in a ZIP file. Then, open the Arduino IDE, Sketch, library, install library in zip file...
